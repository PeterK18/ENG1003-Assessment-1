"use script";

let heightUser = 0;
let angleBuilding = 0;
let angleBase = 0;

let distance = heightUser/  tan(angleBase);

let heightBuilding = distance * tan(angleBuilding);

let totalHeight = heightBuilding + heightUser;
